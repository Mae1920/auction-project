version: "3.9"

services:
  ledger-node:
    build: ./ledger-node
    ports:
      - "5001:5001"

  auction-api:
    build: ./auction-api
    ports:
      - "5000:5000"
    depends_on:
      - ledger-node